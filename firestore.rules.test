rules_version = '2';

// TEST / DEV ONLY — do not deploy to production.
// Allows any authenticated user full access for local prototyping.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
